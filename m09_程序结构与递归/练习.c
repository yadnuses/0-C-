/* ============================================================
 * M9 程序结构与递归 —— 练习（自己动手写）
 * ------------------------------------------------------------
 * 下面每个练习只给"思路提示"，不给完整代码。自己照着思路把
 * 代码补在 TODO 下面。卡住了就回去翻讲义对应小节，或把你写的
 * 代码 + 报错发我。
 *
 * 编译运行：
 *   gcc 练习.c -o lianxi
 *   ./lianxi
 *
 * 建议：一次只做一题，写完就编译运行验证，再做下一题。
 * 递归题先在草稿纸上把前几层"俯冲—返回"的过程画一遍再下笔。
 * ============================================================ */

#include <stdio.h>

/* --------------------------------------------------------
 * 练习3 要用的带参宏：MIN(a,b) 求较小值（对应讲义 六）
 * 思路：照三目运算符 a<b?a:b 写。⚠️ 黄金法则：每个参数加括号，
 *       整个宏体外面再套一层括号，否则遇到表达式实参会算错。
 *       ⚠️ #define 行尾不要加分号。
 * -------------------------------------------------------- */
// TODO: #define MIN(a, b) ...

/* --------------------------------------------------------
 * 练习1 要用的递归函数声明（对应讲义 五）
 * 思路：斐波那契 fib(1)=1, fib(2)=1, fib(n)=fib(n-1)+fib(n-2)。
 * -------------------------------------------------------- */
// TODO: 在这里写 fib 的函数声明（别忘了分号）


int main(void)
{
    /* --------------------------------------------------------
     * 练习1：打印斐波那契前 10 项（对应讲义 五）
     * 思路：循环 i 从 1 到 10，每次调用 fib(i) 打印。
     *       （递归版会重复计算，项数小没关系，先体会递归怎么用。）
     * -------------------------------------------------------- */
    printf("斐波那契前10项: ");
    for (int i = 1; i <= 10; i++) {
        // TODO: 调用 fib(i) 并打印（用空格隔开）
    }
    printf("\n");

    /* --------------------------------------------------------
     * 练习2：递归实现汉诺塔，并数出移动次数（对应讲义 三）
     * 思路：照讲义的三步法写 hanoi 函数（终止条件 n==1 直接搬）。
     *       数次数：可以用一个全局变量，每搬一步就 +1；
     *       搬完和 2^n-1 对一下，验证公式（4 个盘子应是 15）。
     * -------------------------------------------------------- */
    // TODO: 调用你写的 hanoi 搬 3 个盘子，打印移动步骤和总次数


    /* --------------------------------------------------------
     * 练习3：用带参宏 MIN 求最小值（对应讲义 六）
     * 思路：直接 printf 打印 MIN(8, 3) 的结果，应是 3。
     *       进阶：试试 MIN(2+1, 4)，体会括号为什么重要。
     * -------------------------------------------------------- */
    // TODO: 打印 MIN(8, 3)


    return 0;
}

/* ↓↓↓ 在下面补全递归函数定义 ↓↓↓ */

/* 思路：fib —— 终止条件 n<=2 返回 1；否则返回 fib(n-1)+fib(n-2)。 */
// TODO: 定义 fib

/* 思路：hanoi(n, from, via, to) —— n==1 直接 from->to 并 return；
 *       否则：hanoi(n-1, from, to, via)；搬 from->to；hanoi(n-1, via, from, to)。
 *       注意后三个柱子参数的顺序别搞混。 */
// TODO: 定义 hanoi（练习2用）
