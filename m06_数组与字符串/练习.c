/* ============================================================
 * M6 数组与字符串 —— 练习（自己动手写）
 * ------------------------------------------------------------
 * 下面每个练习只给"思路提示"，不给完整代码。自己照着思路把
 * 代码补在 TODO 下面。卡住了就回去翻讲义对应小节，或把你写的
 * 代码 + 报错发我。
 *
 * 编译运行：
 *   gcc 练习.c -o lianxi
 *   ./lianxi
 *
 * 建议：一次只做一题，写完就编译运行验证，再做下一题。
 * ============================================================ */

#include <stdio.h>

int main(void)
{
    /* --------------------------------------------------------
     * 练习1：求最大值和平均值（对应讲义 四套路1、2）
     * 思路：先读 5 个整数进数组（读元素 scanf 要加 &）。
     *       求和用累加器（先清 0），求最值用打擂台（先拿第一个当擂主）。
     *       ⚠️ 平均值要除小数，否则整数除法会砍掉小数。
     * -------------------------------------------------------- */
    int a[5];
    printf("输入5个整数: ");
    for (int i = 0; i < 5; i++) {
        scanf("%d", &a[i]);          /* 读数组元素，&a[i] 不能少 */
    }
    // TODO: 遍历 a 求最大值和平均值并打印


    /* --------------------------------------------------------
     * 练习2：数组逆序输出（对应讲义 三）
     * 思路：只要"从最后一个下标往前打印"即可——下标怎么从大走到小？
     *       想清楚最后一个下标是几（不是 5）、循环走到几为止。
     * -------------------------------------------------------- */
    // TODO: 把 a 从后往前打印一遍


    /* --------------------------------------------------------
     * 练习3：不用 strlen，自己数字符串长度（对应讲义 六）
     * 思路：字符串末尾有个 '\0' 当结束标志。从下标 0 往后走，
     *       只要当前字符还不是 '\0' 就继续，顺便计数，碰到 '\0' 停。
     * -------------------------------------------------------- */
    char s[100];
    printf("输入一个英文单词: ");
    scanf("%s", s);                  /* 字符串不加 &（数组名本身是地址） */
    int len = 0;
    // TODO: 用循环数出 s 的长度（遇到 '\0' 停），结果存进 len
    printf("长度 = %d\n", len);


    /* --------------------------------------------------------
     * 练习4（挑战）：判断 s 是不是回文串（正读反读一样，如 level）
     * 思路：用两个下标，一个从开头(0)、一个从末尾(len-1)，
     *       同时往中间靠，逐个比较对应字符。只要有一对不相等
     *       就不是回文；全程都相等就是回文。
     *       （能用到练习3 数出的 len）
     * -------------------------------------------------------- */
    // TODO: 判断并打印 "是回文" / "不是回文"


    return 0;
}
