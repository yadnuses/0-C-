/* ============================================================
 * M3 循环结构 —— 练习（自己动手写）
 * ------------------------------------------------------------
 * 下面每个练习只给"思路提示"，不给完整代码。自己照着思路把
 * 代码补在 TODO 下面。卡住了就回去翻讲义对应小节，或把你写的
 * 代码 + 报错发我。
 *
 * 编译运行：
 *   gcc 练习.c -o lianxi
 *   ./lianxi
 *
 * 建议：一次只做一题，写完就编译运行验证，再做下一题。
 * 没用到的题目可以先注释掉，避免一堆未完成代码互相干扰编译。
 * ============================================================ */

#include <stdio.h>

int main(void)
{
    /* --------------------------------------------------------
     * 练习1：求 1~n 所有偶数的和（对应讲义 二、三）
     * 思路：准备一个初值为 0 的累加器，用 for 从 1 跑到 n，
     *       遇到偶数（% 2 == 0）就加进累加器。
     * -------------------------------------------------------- */
    int n;
    printf("练习1：求 1~n 所有偶数的和\n");
    printf("输入 n: ");
    scanf("%d", &n);
    // TODO：声明累加器 sum，初值 0
    //       for 循环从 1 到 n
    //       如果 i 是偶数（i % 2 == 0），加到 sum
    //       最后打印 sum


    /* --------------------------------------------------------
     * 练习2：判断一个数是不是素数（对应讲义 六）
     * 思路：素数只能被 1 和自己整除。用一个"标志变量"先假设它是
     *       素数，再从 2 试除到 num/2，一旦发现能整除就推翻假设
     *       （置 0）并 break。循环结束后看标志决定打印结果。
     *       ⚠️ 注意 1 和小于 1 的数不是素数，单独处理。
     * -------------------------------------------------------- */
    int num;
    printf("\n练习2：判断素数\n");
    printf("输入一个整数: ");
    scanf("%d", &num);
    // TODO：如果 num <= 1，直接输出"不是素数"
    //       声明标志 isPrime = 1
    //       for 循环从 2 到 num/2
    //       如果 num % i == 0，isPrime = 0 并 break
    //       循环后根据 isPrime 输出结果


    /* --------------------------------------------------------
     * 练习3：打印 n 行直角三角形（对应讲义 七）
     * 思路：双重循环。外层控制行数，内层控制每行打几个 *。
     *       想让第 i 行打 i 个，内层条件就用 j <= i。
     *       每打完一行别忘了换行。
     * -------------------------------------------------------- */
    int rows;
    printf("\n练习3：打印直角三角形\n");
    printf("输入行数: ");
    scanf("%d", &rows);
    // TODO：外层循环 for (int i = 1; i <= rows; i++)
    //         内层循环 for (int j = 1; j <= i; j++) 打印 *
    //         每行结束后换行


    /* --------------------------------------------------------
     * 练习4（思考题）：先猜输出，再编译验证（对应讲义 六）
     * 思路：在草稿纸上一轮一轮推。i==2 触发 continue（跳过本轮
     *       打印），i==4 触发 break（直接退出）。
     * 你的预测：__________
     * -------------------------------------------------------- */
    printf("\n练习4 的实际输出：");
    for (int i = 0; i < 5; i++) {
        if (i == 2) continue;
        if (i == 4) break;
        printf("%d ", i);
    }
    printf("\n");


    /* --------------------------------------------------------
     * 练习5（进阶）：把练习1的 for 改写成 while（对应讲义 四）
     * 思路：for 的三段（初始化/条件/步进）拆开放——初始化放循环
     *       前面，条件放 while() 括号里，步进放循环体最后一行。
     *       ⚠️ 千万别漏了步进，否则死循环。
     * -------------------------------------------------------- */
    printf("\n练习5：用 while 重写偶数求和\n");
    // TODO：把练习1的 for 循环改成 while 实现
    //       参考讲义四：int i = 1; while (i <= n) { ... i++; }


    /* ============================================================
     * 以下为本次新增练习
     * ============================================================ */

    /* --------------------------------------------------------
     * 练习6：用 do-while 统计一个整数的位数（对应讲义 五）
     * 思路：用 do-while 反复除以 10，每除一次 count++，
     *       直到 number 变成 0。
     *       为什么用 do-while？因为输入 0 时它本身也是 1 位数，
     *       while 会先判断为假直接跳过，结果出错。
     *       ⚠️ 注意负数要先取绝对值。
     * -------------------------------------------------------- */
    int number;
    printf("\n练习6：统计整数位数\n");
    printf("输入一个整数: ");
    scanf("%d", &number);
    // TODO：如果 number < 0，取绝对值（number = -number）
    //       do { number /= 10; count++; } while (number != 0);
    //       打印 count


    /* --------------------------------------------------------
     * 练习7：求 1! + 2! + 3! + ... + n!（对应讲义 三，累乘嵌套）
     * 思路：这个题常考。外层循环从 1 到 n，每轮到 i 时，
     *       内层循环计算 i 的阶乘（累乘），再加到总和里。
     *       ⚠️ 注意每轮开始前累乘器要重置为 1。
     * -------------------------------------------------------- */
    int m;
    printf("\n练习7：求 1! + 2! + ... + n!\n");
    printf("输入 n: ");
    scanf("%d", &m);
    // TODO：声明 total_sum = 0
    //       for (int i = 1; i <= m; i++) {
    //           int fact = 1;
    //           for (int j = 1; j <= i; j++) fact *= j;
    //           total_sum += fact;
    //       }
    //       打印 total_sum


    /* --------------------------------------------------------
     * 练习8（思考题）：输出所有的水仙花数（对应讲义 三、九）
     * 水仙花数：一个三位数，各位数字的立方和等于它本身。
     * 例如 153 = 1^3 + 5^3 + 3^3。
     * 思路：用 for 遍历 100~999，对每个数用 %10 和 /10 拆出
     *       百位、十位、个位，判断立方和是否等于自身。
     * -------------------------------------------------------- */
    printf("\n练习8：输出所有水仙花数（三位数）\n");
    // TODO：for (int i = 100; i <= 999; i++)
    //         拆出百位、十位、个位
    //         如果百^3 + 十^3 + 个^3 == i，打印


    /* --------------------------------------------------------
     * 练习9（进阶）：斐波那契数列前 n 项（对应讲义 九②）
     * 思路：前两项固定为 1, 1，从第 3 项起每项等于前两项之和。
     *       用 x1 = 1, x2 = 1 两个变量滚动前进。
     * -------------------------------------------------------- */
    int fn;
    printf("\n练习9：斐波那契数列前 n 项\n");
    printf("输入 n（>= 2）: ");
    scanf("%d", &fn);
    // TODO：先打印 x1 和 x2
    //       for (int i = 3; i <= fn; i++)
    //          x = x1 + x2
    //          打印 x
    //          x1 = x2; x2 = x;  // 往前挪


    /* --------------------------------------------------------
     * 练习10（挑战）：穷举百钱买百鸡（对应讲义 九④）
     * 公鸡 5 文一只，母鸡 3 文一只，小鸡三只 1 文。
     * 100 文买 100 只鸡，有几种买法？
     * 思路：用嵌套循环枚举公鸡和母鸡的数量，小鸡数 = 100 - 公鸡 - 母鸡，
     *       判断总钱数是否等于 100。
     *       ⚠️ 小鸡三只 1 文，所以小鸡钱数 = 小鸡数 / 3.0
     * -------------------------------------------------------- */
    printf("\n练习10：百钱买百鸡\n");
    // TODO：for 公鸡从 0 到 20
    //         for 母鸡从 0 到 33
    //           小鸡 = 100 - 公鸡 - 母鸡
    //           如果小鸡 >= 0 且 公鸡*5 + 母鸡*3 + 小鸡/3.0 == 100
    //             打印一种方案


    return 0;
}
