/* ============================================================
 * M4 函数 —— 练习（自己动手写）
 * ------------------------------------------------------------
 * 下面每个练习只给"思路提示"，不给完整代码。自己照着思路把
 * 代码补在 TODO 处。卡住了就回去翻讲义对应小节，或把你写的
 * 代码 + 报错发我。
 *
 * ⚠️ 关于函数写在哪：可以把定义写在 main 后面，但要记得在
 *    main 前面先写"函数声明"（带分号！），否则电脑不认识。
 *    嫌麻烦也可以把定义直接写在 main 前面，就不用声明了。
 *
 * 编译运行：
 *   gcc 练习.c -o lianxi
 *   ./lianxi
 * ============================================================ */

#include <stdio.h>

/* 在这里写函数声明（如果你把定义放在 main 后面的话）
 * 思路：声明 = 函数首部 + 分号，例如  int myMax(int a, int b);
 */
// TODO: 按需声明你下面要用到的函数


int main(void)
{
    /* --------------------------------------------------------
     * 练习1：写函数 myMax(int a, int b) 求较大值（对应讲义 二、四）
     * 思路：返回类型 int；函数体里比一下 a、b 谁大，return 大的那个。
     *       main 里调用 myMax(12, 7)，把返回值打印出来。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习2：写 void 函数 printStars(int n) 打印 n 个星号（对应讲义 五）
     * 思路：返回类型 void（只干打印这件事，不还值）；
     *       函数体里用一个循环打 n 个 '*'，最后换行。
     *       ⚠️ void 函数调用时只能单独成一句：printStars(5);
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习3：写带 static 的计数函数，连调三次（对应讲义 九）
     * 思路：函数里用 static int 记录被调用了几次，每次 +1 并打印。
     *       连续调用三次，观察输出是不是 1、2、3。
     *       （对比：如果去掉 static，输出会变成什么？动手试试。）
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习4：亲手验证"传值改不到外面"（对应讲义 七）
     * 思路：写个 swap(int a, int b) 在函数内交换两个形参，
     *       main 里定义两个变量传进去，调用后再打印这两个变量。
     *       重点是观察：外面的变量到底变没变？想清楚为什么。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习5（挑战）：用递归写阶乘 fact(n)（对应讲义 十一）
     * 思路：n! = n × (n-1)!，先想清楚"什么时候该停"（终止条件），
     *       再写"自己调用更小的自己"那一步。算 fact(5) 验证是 120。
     *       ⚠️ 没有终止条件会无限递归崩溃，先把出口写好。
     * -------------------------------------------------------- */
    // TODO


    return 0;
}

/* ↓↓↓ 如果你把函数定义放在 main 后面，就写在这里 ↓↓↓ */
