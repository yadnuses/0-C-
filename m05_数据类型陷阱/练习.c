/* ============================================================
 * M5 数据类型陷阱 —— 练习（自己动手写）
 * ------------------------------------------------------------
 * 这一章是选择题重灾区，练法和别的模块不太一样：
 * 很多题先"猜结果"，再写代码验证。猜错的地方就是你的考点。
 * 下面每题只给"思路提示"，不给完整代码也不给答案，
 * 自己把代码补在 TODO 下面，运行后对照自己的预测。
 *
 * 编译运行：
 *   gcc 练习.c -o lianxi
 *   ./lianxi
 *
 * 建议：先在纸上写下预测，再编译运行，对答案。
 * ============================================================ */

#include <stdio.h>

int main(void)
{
    /* --------------------------------------------------------
     * 练习1：整数除法 vs 强制转换（对应讲义 七）
     * 先猜，再写代码验证：
     *   int a = 10, b = 3;
     *   a / b 是几？      预测: ____
     *   a % b 是几？      预测: ____
     *   (double)a / b 是几？ 预测: ____
     * 思路：整数除法砍小数；想要小数得让算式里有小数(或强转一个)。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习2：char 溢出回绕（对应讲义 四）
     * 先猜，再验证：
     *   unsigned char uc = 255; uc = uc + 1; 打印 uc(%d) 预测: ____
     *   char c = 127;           c = c + 1;   打印 c(%d)  预测: ____
     * 思路：盒子装到头会"绕圈"（想想里程表归零）。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习3：强制转换截断（对应讲义 七）
     * 输入一个小数，用 (int) 转换后打印，看小数部分怎么处理。
     * 思路：注意 (int) 是"砍掉"小数，不是四舍五入。
     *       试试 3.99 和 9.01，验证它俩转完都不进位。
     * -------------------------------------------------------- */
    double d;
    printf("输入一个小数: ");
    scanf("%lf", &d);
    // TODO


    /* --------------------------------------------------------
     * 练习4：自增预测（高频考点，对应讲义 八）
     * 先在纸上算出 r 和 m，再写代码验证（别先看运行结果！）：
     *   int m = 1;
     *   int r = m++ + ++m;
     *   r = ?   m = ?
     * 思路：m++ 用的是旧值，++m 用的是加完的新值。一步步推。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习5：字符与整数互通（对应讲义 六）
     * 把字符 '7' 转成整数 7 并打印。
     * 思路：字符数字减去 '0' 就得到它代表的真实数字。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习6：八进制陷阱（对应讲义 二）
     * 打印 010 和 0x10，看它们到底是几。
     * 思路：0 开头是八进制，0x 开头是十六进制，都不是你以为的十进制。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习7：区间判断的坑（对应讲义 八）
     * 取 x = 100，先打印 (3 <= x <= 5) 的结果，猜猜是几？
     * 再用正确写法判断"x 是否在 3~5 之间"并打印。
     * 思路：3<=x<=5 是从左往右算的，会恒为 1；正确写法要用 && 连两个条件。
     * -------------------------------------------------------- */
    // TODO


    return 0;
}
