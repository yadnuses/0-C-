/* ============================================================
 * M8 结构体 —— 练习（自己动手写）
 * ------------------------------------------------------------
 * 下面每个练习只给"思路提示"，不给完整代码。自己照着思路把
 * 代码补在 TODO 下面。卡住了就回去翻讲义对应小节，或把你写的
 * 代码 + 报错发我。
 *
 * 编译运行：
 *   gcc 练习.c -o lianxi
 *   ./lianxi
 *
 * 建议：一次只做一题，写完就编译运行验证，再做下一题。
 * ============================================================ */

#include <stdio.h>
#include <string.h>      /* 给字符串成员赋值用 strcpy，要包含它 */

/* --------------------------------------------------------
 * 练习1 需要的结构体类型，请定义在这里（main 外面）
 * 思路：一本书有 书名、价格、库存 三样不同类型的信息，
 *       正好打包成一个结构体。书名是字符串（char 数组），
 *       价格是小数，库存是整数。
 *       ⚠️ 别忘了结构体定义结尾的分号。
 * -------------------------------------------------------- */
// TODO: 定义 struct Book { 书名; 价格; 库存; };


int main(void)
{
    /* --------------------------------------------------------
     * 练习1：创建一本书并用点 . 打印它的三个成员（对应讲义 三、四）
     * 思路：声明一个 struct Book 变量，初始化时按成员顺序
     *       用大括号填好三个值，再用"变量.成员"逐个打印。
     *       注意占位符要配成员类型：字符串 %s、小数 %f、整数 %d。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习2：用结构体指针修改库存（对应讲义 八）
     * 思路：定义一个指向上面那本书的指针（用 & 取地址），
     *       通过"指针->成员"把库存减 1（卖出一本），再打印。
     *       ⚠️ 指针访问成员用箭头 ->，不是点 .。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习3：结构体数组——找出年龄最小的学生（对应讲义 九）
     * 数组已给好，你来填遍历逻辑。
     * 思路：打擂台找最小——先假设第 0 个最小，用 for 把每个
     *       学生的 age 和当前最小比，更小就记下它的下标。
     *       最后用记下的下标打印那个学生的姓名和年龄。
     * -------------------------------------------------------- */
    struct Student {
        char name[20];
        int  age;
    };
    struct Student arr[3] = {
        {"Amy", 22},
        {"Bob", 19},
        {"Cindy", 25}
    };
    // TODO: 遍历 arr，找出 age 最小的学生，打印其姓名和年龄


    return 0;
}
