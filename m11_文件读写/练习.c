/* ============================================================
 * M11 文件读写 —— 练习（自己动手写）
 * ------------------------------------------------------------
 * 下面每个练习只给"思路提示"，不给完整代码。自己照着思路把
 * 代码补在 TODO 下面。卡住了就回去翻讲义对应小节，或把你写的
 * 代码 + 报错发我。
 *
 * 编译运行：
 *   gcc 练习.c -o lianxi
 *   ./lianxi
 *
 * ⚠️ 每次 fopen 之后都要先判 NULL；写完/读完都要 fclose。
 * 建议：一次只做一题，写完就编译运行验证，再做下一题。
 * 没用到的题目可以先注释掉，避免一堆未完成代码互相干扰编译。
 * ============================================================ */

#include <stdio.h>

int main(void)
{
    /* --------------------------------------------------------
     * 练习1：把数字 10 20 30 40 50 写入 numbers.txt（对应讲义 三、五、七）
     * 思路：用 "w" 模式 fopen 打开 → 判 NULL → for 循环 fprintf 逐个写
     *       → fclose 关闭。
     *       ⚠️ "w" 会清空原文件；忘了 fclose 数据可能丢。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习2：从 numbers.txt 读回所有数字，求和并打印（对应讲义 五、六）
     * 思路：用 "r" 模式打开 → 判 NULL → 用 fscanf 循环读，
     *       靠"成功读到的项数"判断是否到结尾（讲义六讲过的正确写法）
     *       → 边读边累加 → fclose → 打印总和。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习3：用追加模式往 numbers.txt 末尾再加一个数 99（对应讲义 四）
     * 思路：换成 "a" 模式打开（不是 "w"，否则前面的数就被清空了），
     *       fprintf 写入 99，fclose。再用练习2的办法读一遍，
     *       确认 99 出现在最后、原来的数还在。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习4（思考题）：为什么读循环推荐用 fscanf(...) 的返回值判断，
     * 而不推荐 while(!feof(fp))？（对应讲义 六）
     * 思路：想一想 feof 是"尝试读失败之后"才变真的，
     *       这会导致最后一条数据怎样？把你的理解写成下面这行注释。
     * -------------------------------------------------------- */
    // 你的答案: ____________________________________________


    return 0;
}
