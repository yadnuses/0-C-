/* ============================================================
 * M7 指针 —— 练习（自己动手写）
 * ------------------------------------------------------------
 * 指针是公认最难的一关，下面每个练习只给"思路提示"，不给完整
 * 代码。自己照着思路把代码补在 TODO 下面。卡住了就回去翻讲义
 * 对应小节，或把你写的代码 + 报错发我。
 *
 * 编译运行：
 *   gcc 练习.c -o lianxi
 *   ./lianxi
 *
 * 建议：一次只做一题，写完就编译运行验证，再做下一题。
 * 没用到的题目可以先注释掉，避免一堆未完成代码互相干扰编译。
 * ============================================================ */

#include <stdio.h>

/* --------------------------------------------------------
 * 练习2 要用：先在这里声明 swap，定义写在 main 后面。
 * 思路：交换要改到外面的变量，所以参数必须是"指针"。
 * -------------------------------------------------------- */
// TODO: 声明 swap（参数是两个 int 指针，无返回值）


int main(void)
{
    /* --------------------------------------------------------
     * 练习1：用指针读写变量（对应讲义 二、三）
     * 思路：定义一个指针指向 n（用 & 取 n 的地址）；
     *       再通过对指针解引用（* 号）把 n 改成 100；
     *       最后打印 n，验证它真的被指针改掉了。
     * -------------------------------------------------------- */
    int n = 5;
    // TODO: 定义指向 n 的指针，通过它把 n 改成 100，再打印 n


    /* --------------------------------------------------------
     * 练习2：用 swap 真正交换两个数（对应讲义 七）
     * 思路：调用时要把 a、b 的"地址"传进去（加 &）；
     *       swap 函数体里用临时变量，通过解引用交换两个地址里的值。
     *       对照 M4 的传值版 swap，体会"传地址才改得动外面"。
     * -------------------------------------------------------- */
    int a = 3, b = 8;
    printf("交换前: a=%d b=%d\n", a, b);
    // TODO: 调用 swap，把 a、b 的地址传进去
    printf("交换后: a=%d b=%d\n", a, b);


    /* --------------------------------------------------------
     * 练习3：用指针（不用下标 []）遍历数组求和（对应讲义 五、六）
     * 思路：让一个指针先指向数组首元素（数组名就是首地址）；
     *       循环里用解引用取当前元素累加，再让指针前移一格指向下一个；
     *       走够 5 个就停。回忆 p+1 是"跳一个元素"而非一个字节。
     * -------------------------------------------------------- */
    int arr[5] = {2, 4, 6, 8, 10};
    // TODO: 用指针遍历 arr 求和并打印


    /* --------------------------------------------------------
     * 练习4：验证 a[i] 和 *(a+i) 完全相同（对应讲义 五）
     * 思路：挑一个下标（比如 3），分别用下标写法和指针写法打印，
     *       看看是不是同一个值。理解"下标本质就是指针运算"。
     * -------------------------------------------------------- */
    // TODO: 打印 arr[3] 和 *(arr+3) 做对比


    /* --------------------------------------------------------
     * 练习5（挑战）：写一个函数同时返回商和余数（对应讲义 七）
     * 思路：return 只能带回一个值，所以让函数多收两个指针参数，
     *       把商和余数分别写回指针指向的变量。调用时传两个变量的地址。
     * -------------------------------------------------------- */
    // TODO: 自己声明+定义 divmod，并在这里调用、打印商和余数


    return 0;
}

/* ↓↓↓ 在下面定义 swap（以及练习5的 divmod）↓↓↓ */
// TODO: 定义 swap —— 用临时变量，通过解引用交换两个指针指向的值
