#include <stdio.h>
#include <stdlib.h>

/* ============================================================
 * M10 指针进阶与链表 —— 练习（自己动手写）
 * ------------------------------------------------------------
 * 下面每个练习只给"思路提示"，不给完整代码。链表全程靠画
 * 箭头图理解，写之前先在纸上画一遍每根指针的指向。卡住了就
 * 回去翻讲义对应小节，或把你写的代码 + 报错发我。
 *
 * 编译运行：
 *   gcc 练习.c -o lianxi
 *   ./lianxi
 *
 * 建议：一次只做一题，写完就编译运行验证，再做下一题。
 * ============================================================ */

/* 链表节点：数据域 + 指针域（指向同类型） */
struct Node {
    int data;
    struct Node *next;
};

int main(void)
{
    /* --------------------------------------------------------
     * 练习1：malloc 动态开数组（对应讲义 一）
     * 思路：用 malloc 申请能放 3 个 int 的空间（sizeof(int)*3），
     *       记得检查返回值是不是 NULL；存入 100,200,300 并打印；
     *       用完 free，再把指针置 NULL。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习2：头插法建链表（对应讲义 三）
     * 思路：空表起步（head = NULL）。循环 1~5，每次 malloc 一个
     *       新节点、填好 data，再用"先接旧头、后移 head"两句头插。
     *       ⚠️ 顺序不能反，反了会断链。头插结果是逆序 5 4 3 2 1。
     * -------------------------------------------------------- */
    struct Node *head = NULL;
    for (int i = 1; i <= 5; i++) {
        // TODO: 造新节点 data=i，头插到 head 前面
    }


    /* --------------------------------------------------------
     * 练习3：遍历打印链表（对应讲义 三）
     * 思路：cur 从 head 出发，while(cur != NULL) 里打印 cur->data，
     *       然后 cur = cur->next 挪到下一个。⚠️ 不能写 cur++。
     *       接着练习2，应输出 5 4 3 2 1。
     * -------------------------------------------------------- */
    printf("链表: ");
    // TODO: 遍历打印 head
    printf("\n");


    /* --------------------------------------------------------
     * 练习4：统计链表长度（对应讲义 三）
     * 思路：遍历一遍，每经过一个节点计数器 +1，最后打印。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习5（挑战）：在链表中查找值为 3 的节点（对应讲义 三）
     * 思路：遍历时比较 cur->data == 3，找到就打印"找到了"并 break；
     *       走到 NULL 还没找到就打印"没找到"。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习6（收尾）：释放整条链表（对应讲义 三）
     * 思路：循环 free 每个节点。⚠️ free 当前节点前，要先用临时
     *       指针记住 next，否则 free 完就找不到后面了。
     * -------------------------------------------------------- */
    // TODO


    return 0;
}
