/* ============================================================
 * M2 分支结构 —— 练习（自己动手写）
 * ------------------------------------------------------------
 * 下面每个练习只给"思路提示"，不给完整代码。自己照着思路把
 * 代码补在 TODO 下面。卡住了就回去翻讲义对应小节，或把你写的
 * 代码 + 报错发我。
 *
 * 编译运行：
 *   gcc 练习.c -o lianxi -lm   // 练习5、7 可能用到 math.h，加 -lm 保险
 *   ./lianxi
 *
 * 建议：一次只做一题，写完就编译运行验证，再做下一题。
 * 没用到的题目可以先注释掉，避免一堆未完成代码互相干扰编译。
 * ============================================================ */

#include <stdio.h>

int main(void)
{
    /* --------------------------------------------------------
     * 练习1：判断正负零（对应讲义 一、二、七）
     * 思路：一个数有三种可能 —— 大于0、小于0、等于0。
     *       三种情况 → 用 if / else if / else 三分支。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习2：输入分数，输出等级 A/B/C/D（对应讲义 七）
     * 思路：90以上A、80~89B、60~79C、其余D，是典型的 else if 阶梯。
     *       ⚠️ 条件顺序要"由大到小"，否则宽松条件会先把分数截胡。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习3：输入 1~7，用 switch 输出星期几（对应讲义 十）
     * 思路：拿星期数去 switch 对号入座，每个 case 打印对应中文。
     *       ⚠️ 每个 case 末尾别忘了 break，再写个 default 兜底。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习4：改错（对应讲义 五）
     * 下面这段想判断 x 是不是 0，但写错了，运行结果不对。
     * 思路：先想清楚 = 和 == 的区别，找出 bug 在哪，改对它。
     *       （动手改完编译跑一下，看看改之前/之后输出差别。）
     * -------------------------------------------------------- */
    int x = 3;
    if (x = 0) {                  /* ← 这里有 bug，请改正 */
        printf("x 是 0\n");
    } else {
        printf("x 不是 0\n");
    }


    /* --------------------------------------------------------
     * 练习5：输入年份，判断是否闰年（对应讲义 八）
     * 思路：闰年规则 = 能被4整除且不能被100整除，"或者"能被400整除。
     *       把"且""或"翻译成 && 和 ||，注意用括号把两部分分清楚。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习6：用三目运算符求两个数的较大值（对应讲义 十一）
     * 思路：max = 条件 ? 真时取的值 : 假时取的值。一行写完。
     * -------------------------------------------------------- */
    // TODO


    /* --------------------------------------------------------
     * 练习7（体会，不算分）：故意制造 switch 贯穿（对应讲义 十）
     * 思路：写个小 switch，把某个 case 的 break 故意删掉，运行看看
     *       输出是不是"贯穿"到了后面的 case。体会 break 的作用。
     * -------------------------------------------------------- */
    // TODO


    return 0;
}
