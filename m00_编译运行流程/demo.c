#include <stdio.h>

int main(void)
{
    printf("Hello C! 我的第一个程序跑起来了。\n");
    printf("从写代码到跑起来，要经过：编辑 -> 编译 -> 运行。\n");

    int year = 2026;
    printf("现在是 %d 年，我开始学C语言了。\n", year);

    return 0;
}
